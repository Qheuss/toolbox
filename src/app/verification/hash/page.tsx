import Hash from '@/components/verification/hash/Hash';

const page = () => {
  return (
    <div className='flex flex-col items-center justify-center min-h-screen container mx-auto'>
      <Hash />
    </div>
  );
};

export default page;
